# Forge — Multi-model autonomous development pipeline
# Minimal dependencies. The heavy lifting is done by CLIs and SDKs.

# Deep Think SDK (Gemini 3.1 Pro with ThinkingLevel.HIGH)
# Your Google AI Pro Ultra subscription covers the usage.
google-genai>=1.50.0

# Environment variable loading
python-dotenv>=1.0.0

# Dashboard (NiceGUI — Python-native web UI with WebSocket real-time)
nicegui>=2.0

# HTTP client for OTA update manager (Supabase Storage downloads)
httpx>=0.27

# Native desktop window (wraps NiceGUI in a real OS window via pywebview)
pywebview>=5.0

# Note: The following are NOT listed because they're CLI tools,
# not Python packages:
#   - claude (Claude Code CLI) — install via npm: npm install -g @anthropic-ai/claude-code
#   - gemini (Gemini CLI) — install via npm: npm install -g @anthropic-ai/gemini-cli
#
# Both require active subscriptions:
#   - Claude Max (Anthropic) — unlimited Claude Code usage
#   - Google AI Pro Ultra — unlimited Gemini CLI + Deep Think usage